#include <stdio.h>

struct User {
    int id;
    int age;
    int weight = 10;
};

int main(void) {
    // struct User u;
    // u.id = 1;
    // u.age = 25;
    // u.weight = 75;
    //
    // char a[100] = "Hello World";
    // printf("%s\n", a);
    // printf("%d %d %d\n", u.id, u.age, u.weight);
    //
    // return 0;
}
